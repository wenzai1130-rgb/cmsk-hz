/**
 * 项目分析（ProjectList）页面 · 需求说明配置
 * 挂载到全局「需求抽屉」，供 PM / 业务侧查阅字段口径与交互规则。
 */

import type { RequirementItem } from "@/components/requirements";

export const PAGE_REQUIREMENTS: RequirementItem[] = [
  {
    code: "REQ-01",
    moduleId: "project-list-header",
    title: "页面标题、搜索框与最近浏览",
    desc: [
      '【展示内容】① 居中展示搜索输入框，占位文案"搜索项目名称"。② 搜索框下方展示「最近浏览：」标签 + 最多 3 个最近访问过的项目胶囊标签（History 图标 + 项目名称），灰色背景圆角胶囊样式。',
      '【交互规则】① 搜索输入时实时过滤下方项目卡片列表（忽略大小写，匹配项目名称）；输入非空时右侧显示清除按钮，点击清空。② 点击最近浏览胶囊直接跳转到对应项目详情页；悬浮时背景加深。',
      '【数据规则】① 搜索关键字与其他筛选条件为「与」关系。② 最近浏览按用户最近访问项目倒序排列，最多展示 3 条。',
      '【边界处理】① 搜索结果为空时展示"当前筛选下暂无项目"空状态。② 无最近浏览记录时，「最近浏览：」标签仍然显示，右侧改为一枚灰色虚线边框的占位胶囊（History 图标 + 文案"暂无最近浏览记录，点击项目卡片查看后将自动记录"），不可点击，用于引导用户产生浏览行为。',
    ].join("\n"),

  },
  {
    code: "REQ-02",
    moduleId: "project-list-filter",
    title: "筛选条件",
    desc: [
      '【展示内容】筛选卡片分两层：\n· 第 1 层（顶部工具条）：城市公司多选下拉（默认按当前登录用户权限过滤 ）、口径下拉（全口径-权益 / 全口径）、数据日期选择器（默认昨日）。\n· 第 2 层（筛选行）：业态类型（单选：全部 / 住宅 / 商业 / 公寓 / 写字楼 / 车位 / 配套及其他）、项目阶段（单选：全部 / 未开盘 / 新盘 / 持销 / 尾盘）。',
      '【交互规则】① 城市公司支持多选：点击"清空"清空所有已选城市；点击具体城市在已选/未选间切换；空选中态视为"全部"。② 业态类型、项目阶段为单选切换。③ 口径与数据日期为单选下拉，切换后立即刷新列表。④ 任一筛选条件变更立即刷新下方项目列表并重置分页至第 1 页。',
      '【数据规则】① 城市公司列表默认排序，按能级顺序排列：一线 → 新一线 → 二线 → 三四线及以下、剩余货值从高到低。② 项目阶段判定顺序：首先判定是否为"未开盘"（未取证/未推售，当前按 projectId 稳定哈希约 12% 项目模拟为未开盘）；其余项目中，去化率 ≥ 95% 判为"尾盘"，去化率 ≤ 30% 判为"新盘"，其余为"持销"。③ 数据日期默认取昨日快照。',
      '【边界处理】当前登录用户为某城市公司权限时，强制锁定为该城市，其他城市置灰不可选，数据强制过滤为该城市。',
    ].join("\n"),
  },
  {
    code: "REQ-03",
    moduleId: "project-list-sort",
    title: "排序与视图切换",
    desc: [
      '【展示内容】筛选卡片下方一排工具条：\n· 左侧排序项按顺序排列：开盘时间、剩余货值、项目去化率；当前激活项蓝色高亮并显示升/降箭头，未激活项显示灰色双向箭头。\n· 右侧：当有任一筛选条件激活时显示「清空条件」按钮（橡皮擦图标）；始终显示「卡片 / 表格」视图切换分段控件。',
      '【交互规则】① 点击"开盘时间" / "剩余货值" / "项目去化率"：若当前已激活则切换升/降；否则切换到该字段并使用默认方向（开盘时间降序即最新开盘在前、剩余货值降序、项目去化率升序）。② 点击「清空条件」一键重置城市公司、业态类型、项目阶段筛选（不影响排序、口径、日期）。③ 点击「卡片 / 表格」切换列表呈现方式。',
      '【数据规则】页面默认排序为「开盘时间」降序（最新开盘在前）；无开盘时间的项目统一排在有开盘时间的项目之后，并在该组内按「拿地时间」排序（默认降序，即最新拿地在前）；切换筛选条件时保留当前排序。',
      '【边界处理】切换排序或视图时保留当前筛选条件；切换排序时重置分页至第 1 页；无激活筛选条件时隐藏「清空条件」按钮。',
    ].join("\n"),
  },

  {
    code: "REQ-04",
    moduleId: "project-list-cards",
    title: "项目列表（卡片 / 表格）",
    desc: [
      '【展示内容】排序栏上方展示"为您找到 N 个项目"计数（数字为品牌蓝色）。\n\n· 卡片视图：响应式网格（移动端 1 列 / md 2 列 / lg 3 列），每张卡片分两块：\n\n  - 上半部分左侧：项目名称（可点击跳转详情）+ 复制按钮 + 项目阶段徽标 + 业态徽标 + 区域路径（城市 · 区县 · 街道，MapPin 图标）+ 开盘时间（CalendarDays 图标，格式 YYYY-MM-DD；无开盘时间、项目阶段为"未开盘"或"尾盘"时显示"--"）。\n\n  - 上半部分右侧：独立的浅蓝卡片，标题"项目去化率"+ 大号品牌蓝百分比数值 + 底部浅→深蓝渐变进度条。\n\n  - 下半部分（顶部分隔线）：四个指标平铺——剩余货值(亿)、销售均价(元/㎡)、总套数(套)、未售套数(套，橙色高亮)。\n\n· 表格视图：白色表格，列依次为——项目名称（含区县·街道副行）、城市公司、开盘时间、业态、阶段、均价(元/㎡)、去化率、剩余货值(亿)。',
      '【交互规则】① 卡片整体可点击跳转项目详情；悬停时边框变品牌蓝、阴影加深、微向上位移。② 项目名称旁复制按钮点击后复制项目全名到剪贴板，事件阻止冒泡不触发跳转。③ 表格行悬停浅灰底色；项目名点击跳转详情。',
      '【数据规则】① 去化率保留 2 位小数；剩余货值单位"亿"，保留 2 位小数；销售均价单位"元/㎡"，四舍五入到整数并按千分位展示。② 阶段徽标颜色：未开盘橙色、新盘绿色、持销蓝色、尾盘灰色。',
      '【边界处理】筛选下无项目时，两种视图均展示"当前筛选下暂无项目"空态，并隐藏分页器。',
    ].join("\n"),
  },
  {
    code: "REQ-05",
    moduleId: "project-list-pagination",
    title: "分页器",
    desc: [
      '【展示内容】列表底部展示：左侧"第 X-Y 条 / 共 N 条"计数；右侧翻页控件（上一页 / 页码 / 下一页）与每页条数选择器（10 / 20 / 50 / 100 条/页），默认显示20条/页。',
      '【交互规则】① 点击页码或上下页按钮切换当前页；到达首/末页时对应按钮置灰不可点。② 更改每页条数或切换筛选条件、排序时自动回到第 1 页。③ 当前页码使用品牌蓝底白字高亮。',
      '【数据规则】页码超过 7 页时使用省略号（1 … 4 5 6 … 20）紧凑展示，两侧始终展示首/末页。',
      '【边界处理】无数据时隐藏分页器整体。',
    ].join("\n"),
  },
];
